#include <bits/stdc++.h>
using namespace std;
string senderSide( string s, int div){
    int n= s.length();
    
    while(div>0){
        
    }

}

int main(){
    string sent,rec;
    int n;
    cin>>sent>>n>>rec;
    int div = sent.length()/n;
    string checksum = senderSide(sent,div);
    return 0;
}